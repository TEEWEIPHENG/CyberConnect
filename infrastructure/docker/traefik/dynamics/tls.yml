tls:

  certificates:

    - certFile: /certificates/cyberconnect.local.pem
      keyFile: /certificates/cyberconnect.local-key.pem

  options:

    default:

      minVersion: VersionTLS13

      sniStrict: true